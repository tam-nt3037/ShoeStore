@{
    ViewBag.Title = "Products";
}
@{List<Week02.Models.Nhom_sp> getNhom_sp = (List<Week02.Models.Nhom_sp>)ViewData["getNhom_sp"];}
<div id="content" class="float_r">
    @{ if (getNhom_sp == null)
        {
            <h1> Products</h1>
        }
        else
        {
            foreach (var p in getNhom_sp)
            {
                <h1>@p.Ten_nhom</h1>
            }
        }
    }
    @model List<Week02.Models.San_pham>
    @foreach (var p in Model)
    {
        <div class="product_box">
            <h3>@p.Ten_sp</h3>
            <a href='@Url.Action("ProductDetail/"+ p.ID_sp + "/" + p.Nsx_sp, "Home")'>
                <img src='@Url.Content("~/images/product/"+p.Hinh_sp+"")' />
            </a>
            <p> @p.Mo_ta </p>
            <p class="product_price">$ @p.Gia_sp</p>
            <a href='@Url.Action("OrderNow", "ShoppingCart", new { id  = p.ID_sp})' class="addtocart"></a>
            @*@Html.ActionLink("Order Now", "OrderNow", "ShoppingCart", new { id = p.ID_sp }, null)*@
            @*<a href='@Url.Action("Order Now1","ShoppingCart","Home", new { id = p.ID_sp },null)' class="addtocart"></a>*@
            <a href='@Url.Action("ProductDetail/"+ p.ID_sp + "/" + p.Nsx_sp, "Home")' class="detail"></a>
        </div>
    }


    @*@for (int i = 0; i < Model.Count(); i++)
        {
            if (i % 3 == 0)
            {
                @Html.Raw("<div class='product_box'>")
            }
            else
            {
                @Html.Raw("<div class='product_box no_margin_right'>")
            }
            foreach (var p in Model)
                {
                <h3>@p.Ten_sp</h3>
                <a href='@Url.Action("ProductDetail", "Home")'>
                    <img src='@Url.Content("~/images/product/" + p.Hinh_sp + "")' />
                </a>
                <p> @p.Mo_ta </p>
                <p class="product_price">$ @p.Gia_sp</p>
                <a href='@Url.Action("ShoppingCart", "Home")' class="addtocart"></a>
                <a href='@Url.Action("ProductDetail", "Home")' class="detail"></a>
                @Html.Raw("</div>")
                }
            }*@
</div>
<div class="cleaner"></div>
